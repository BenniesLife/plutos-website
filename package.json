{
  "name": "plutos-website",
  "version": "1.0.0",
  "private": true,
  "description": "Pluto’s Family Bar — a static website on GitHub Pages",
  "scripts": {
    "preview": "python3 -m http.server 4173",
    "test": "playwright test",
    "format:check": "prettier --check index.html styles.css script.js atmosphere.js playwright.config.js tests scripts .github/workflows README.md package.json",
    "stage": "node scripts/stage.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0",
    "prettier": "^3.9.6"
  }
}
